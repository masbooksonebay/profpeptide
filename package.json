{
  "name": "profpeptide",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "node scripts/check-links.mjs && node scripts/check-grids.mjs && node scripts/check-freshness.mjs && node scripts/check-surfaces.mjs && node scripts/gen-vendor-slugs.mjs && next build && next-sitemap",
    "start": "next start",
    "lint": "next lint",
    "check:vendors": "node scripts/check-vendors.mjs",
    "check:links": "node scripts/check-links.mjs",
    "check:grids": "node scripts/check-grids.mjs",
    "check:freshness": "node scripts/check-freshness.mjs",
    "check:surfaces": "node scripts/check-surfaces.mjs"
  },
  "dependencies": {
    "fuse.js": "^7.3.0",
    "next": "14.2.3",
    "next-sitemap": "^4.2.3",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "autoprefixer": "^10.0.1",
    "postcss": "^8",
    "tailwindcss": "^3.4.1",
    "typescript": "^5"
  }
}
